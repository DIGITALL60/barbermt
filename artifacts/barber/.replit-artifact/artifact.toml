kind = "web"
previewPath = "/"
title = "New King Barber"
version = "1.0.0"
id = "artifacts/barber"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21865

[services.development]
run = "pnpm --filter @workspace/barber run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/barber", "run", "build" ]
publicDir = "artifacts/barber/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21865"
BASE_PATH = "/"
